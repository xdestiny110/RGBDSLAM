SET(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/lib)

INCLUDE_DIRECTORIES(
	${OpenCV_INCLUDE_DIRS}
	${PROJECT_SOURCE_DIR}/CommonHeader)
	
AUX_SOURCE_DIRECTORY(. Sim_SRC)
ADD_LIBRARY(ImageSimilarity STATIC ${Sim_SRC})
SET_TARGET_PROPERTIES(ImageSimilarity PROPERTIES OUTPUT_NAME "ImageSimilarity")